{
  "$schema": "https://sse-hooks.vercel.app/api/registry/schema/hook.json",
  "name": "use-ssr",
  "type": "registry:hook",
  "title": "useSSR",
  "description": "Whether code is running in the browser.",
  "dependencies": [],
  "registryDependencies": [],
  "api": {
    "tags": {
      "category": "utilities",
      "public": true,
      "see": "[Documentation](/docs/use-ssr)",
      "example": "```tsx\r\nconst { isBrowser, isServer } = useSSR();\r\nif (isServer) {\r\nreturn <StaticLoader />;\r\n}\r\nreturn <ClientComponent />;\r\n```"
    },
    "types": [],
    "parameters": [],
    "returnType": {
      "name": "UseSSRReturn",
      "description": "Object containing boolean flags for the current environment.",
      "properties": [
        {
          "name": "isBrowser",
          "type": "boolean",
          "isOptional": false,
          "description": "Whether code is running in the browser."
        },
        {
          "name": "isServer",
          "type": "boolean",
          "isOptional": false,
          "description": "Whether code is running on the server."
        },
        {
          "name": "isNative",
          "type": "boolean",
          "isOptional": false,
          "description": "Whether code is running in a Native environment (e.g. React Native)."
        },
        {
          "name": "device",
          "type": "Device",
          "isOptional": false,
          "description": "The detected device type."
        },
        {
          "name": "canUseWorkers",
          "type": "boolean",
          "isOptional": false,
          "description": "Whether Web Workers are supported."
        },
        {
          "name": "canUseEventListeners",
          "type": "boolean",
          "isOptional": false,
          "description": "Whether window.addEventListener is available."
        },
        {
          "name": "canUseViewport",
          "type": "boolean",
          "isOptional": false,
          "description": "Whether window.screen is available."
        }
      ]
    }
  },
  "file": {
    "path": "use-ssr.ts",
    "content": "export enum Device {\n  Browser = \"browser\",\n  Server = \"server\",\n  Native = \"native\",\n}\n\nexport interface UseSSRReturn {\n  /** Whether code is running in the browser. */\n  isBrowser: boolean;\n  /** Whether code is running on the server. */\n  isServer: boolean;\n  /** Whether code is running in a Native environment (e.g. React Native). */\n  isNative: boolean;\n  /** The detected device type. */\n  device: Device;\n  /** Whether Web Workers are supported. */\n  canUseWorkers: boolean;\n  /** Whether window.addEventListener is available. */\n  canUseEventListeners: boolean;\n  /** Whether window.screen is available. */\n  canUseViewport: boolean;\n}\n\nconst { Browser, Server, Native } = Device;\n\nconst canUseDOM = !!(\n  typeof window !== \"undefined\" &&\n  window.document &&\n  window.document.createElement\n);\n\nconst canUseNative: boolean =\n  typeof navigator != \"undefined\" && navigator.product == \"ReactNative\";\n\nconst device = canUseNative ? Native : canUseDOM ? Browser : Server;\n\nconst SSRObject: UseSSRReturn = {\n  isBrowser: device === Browser,\n  isServer: device === Server,\n  isNative: device === Native,\n  device,\n  canUseWorkers: typeof Worker !== \"undefined\",\n  canUseEventListeners: device === Browser && !!window.addEventListener,\n  canUseViewport: device === Browser && !!window.screen,\n};\n\nconst assign = (...args: any[]) =>\n  args.reduce((acc, obj) => ({ ...acc, ...obj }), {});\nconst values = (obj: any) => Object.keys(obj).map((key) => obj[key]);\nconst toArrayObject = (): UseSSRReturn =>\n  assign((values(SSRObject), SSRObject));\n\nlet useSSRObject = toArrayObject();\n\nexport const weAreServer = () => {\n  SSRObject.isServer = true;\n  useSSRObject = toArrayObject();\n};\n\n/**\n * Custom hook that detects the current environment (Browser, Server, or Native)\n * and capability support (Workers, EventListeners). useful for avoiding hydration mismatches.\n * @category utilities\n * @returns {UseSSRReturn} Object containing boolean flags for the current environment.\n * @public\n * @see [Documentation](/docs/use-ssr)\n * @example\n * ```tsx\n * const { isBrowser, isServer } = useSSR();\n * if (isServer) {\n * return <StaticLoader />;\n * }\n * return <ClientComponent />;\n * ```\n */\nexport const useSSR = (): UseSSRReturn => useSSRObject;\n",
    "js": "export var Device;\n(function (Device) {\n  Device[\"Browser\"] = \"browser\";\n  Device[\"Server\"] = \"server\";\n  Device[\"Native\"] = \"native\";\n})(Device || (Device = {}));\nconst { Browser, Server, Native } = Device;\nconst canUseDOM = !!(\n  typeof window !== \"undefined\" &&\n  window.document &&\n  window.document.createElement\n);\nconst canUseNative =\n  typeof navigator != \"undefined\" && navigator.product == \"ReactNative\";\nconst device = canUseNative ? Native : canUseDOM ? Browser : Server;\nconst SSRObject = {\n  isBrowser: device === Browser,\n  isServer: device === Server,\n  isNative: device === Native,\n  device,\n  canUseWorkers: typeof Worker !== \"undefined\",\n  canUseEventListeners: device === Browser && !!window.addEventListener,\n  canUseViewport: device === Browser && !!window.screen,\n};\nconst assign = (...args) => args.reduce((acc, obj) => ({ ...acc, ...obj }), {});\nconst values = (obj) => Object.keys(obj).map((key) => obj[key]);\nconst toArrayObject = () => assign((values(SSRObject), SSRObject));\nlet useSSRObject = toArrayObject();\nexport const weAreServer = () => {\n  SSRObject.isServer = true;\n  useSSRObject = toArrayObject();\n};\nexport const useSSR = () => useSSRObject;\n"
  }
}
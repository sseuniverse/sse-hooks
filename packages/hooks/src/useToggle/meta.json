{
  "$schema": "https://sse-hooks.vercel.app/api/registry/schema/hook.json",
  "name": "use-toggle",
  "type": "registry:hook",
  "title": "useToggle",
  "description": "Custom hook that manages a boolean toggle state in React components.",
  "dependencies": [
    "react"
  ],
  "registryDependencies": [],
  "api": {
    "tags": {
      "category": "state",
      "see": "[Documentation](https://sse-hooks.vercel.app/docs/hooks/use-toggle)",
      "public": true
    },
    "types": [],
    "parameters": [
      {
        "name": "defaultValue",
        "type": "boolean",
        "isOptional": true,
        "description": "- The initial value for the toggle state."
      }
    ],
    "returnType": {
      "name": "[boolean, () => void, Dispatch<SetStateAction<boolean>>]",
      "description": "A tuple containing the current state, a function to toggle the state, and a function to set the state explicitly."
    }
  },
  "file": {
    "path": "use-toggle.ts",
    "content": "/**\n * Custom hook that manages a boolean toggle state in React components.\n *\n * @category state\n * @param {boolean} [defaultValue] - The initial value for the toggle state.\n * @returns {[boolean, () => void, Dispatch<SetStateAction<boolean>>]} A tuple containing the current state, a function to toggle the state, and a function to set the state explicitly.\n * @see [Documentation](https://sse-hooks.vercel.app/docs/hooks/use-toggle)\n * @public\n */\nexport function useToggle(\n  defaultValue?: boolean,\n): [boolean, () => void, Dispatch<SetStateAction<boolean>>] {\n  const [value, setValue] = useState(!!defaultValue);\n\n  const toggle = useCallback(() => {\n    setValue((x) => !x);\n  }, []);\n\n  return [value, toggle, setValue];\n}\n",
    "js": "export function useToggle(defaultValue) {\n  const [value, setValue] = useState(!!defaultValue);\n  const toggle = useCallback(() => {\n    setValue((x) => !x);\n  }, []);\n  return [value, toggle, setValue];\n}\n"
  }
}
---
title: useIsClient
description: Custom hook that determines if the code is running on the client side (in the browser).
createdAt: 2026-02-04T15:43:17.055Z
updatedAt: 2026-02-04T15:43:17.055Z

---

## Installation

::code-group
```bash [terminal]
npx sse-tool add use-is-client
```
```bash [npm]
npm install sse-hooks
```
::

## Usage

::code-group
```tsx [example.ts]
import { useIsClient } from './{hooks file}'

const isClient = useIsClient();
// Use isClient to conditionally render or execute code specific to the client side.
```
```tsx [package.ts]
import { useIsClient } from 'sse-hooks'

const isClient = useIsClient();
// Use isClient to conditionally render or execute code specific to the client side.
```
::

## API


### Return Value

Returns `boolean`.

A boolean value indicating whether the code is running on the client side.

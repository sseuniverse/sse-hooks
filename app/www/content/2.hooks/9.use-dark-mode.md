---
title: useDarkMode
description: Custom hook that returns the current state of the dark mode.
createdAt: 2026-02-04T15:43:16.964Z
updatedAt: 2026-02-04T15:43:16.964Z

---

## Installation

::code-group
```bash [terminal]
npx sse-tool add use-dark-mode
```
```bash [npm]
npm install sse-hooks
```
::

## Usage

::code-group
```tsx [example.ts]
import { useDarkMode } from './{hooks file}'

const { isDarkMode, toggle, enable, disable, set } = useDarkMode({ defaultValue: true });
```
```tsx [package.ts]
import { useDarkMode } from 'sse-hooks'

const { isDarkMode, toggle, enable, disable, set } = useDarkMode({ defaultValue: true });
```
::

## API

### Parameters

| Name | Type | Description |
| :--- | :--- | :--- |
| options | `DarkModeOptions` | The initial value of the dark mode, default `false`. |

### Return Value

Returns `DarkModeReturn`.

An object containing the dark mode's state and its controllers.

---
title: useFetch
description: Custom hook that provides a wrapper around the native [`fetch API`](https://developer.
createdAt: 2026-02-04T15:43:17.008Z
updatedAt: 2026-02-04T15:43:17.008Z

---

## Installation

::code-group
```bash [terminal]
npx sse-tool add use-fetch
```
```bash [npm]
npm install sse-hooks
```
::

## Usage

::code-group
```tsx [example.ts]
import { useFetch } from './{hooks file}'

interface User {
id: number;
name: string;
}

const { data, loading, error, execute } = useFetch<User>('https://api.example.com/user/1', {
immediate: true,
onSuccess: (data) => console.log('User loaded:', data),
});
```
```tsx [package.ts]
import { useFetch } from 'sse-hooks'

interface User {
id: number;
name: string;
}

const { data, loading, error, execute } = useFetch<User>('https://api.example.com/user/1', {
immediate: true,
onSuccess: (data) => console.log('User loaded:', data),
});
```
::

## API

### Parameters

| Name | Type | Description |
| :--- | :--- | :--- |
| url | `string` | The URL to fetch. |
| options | `UseFetchOptions` | Options for customizing the request and hook behavior (optional). |

### Return Value

Returns `UseFetchReturn`.

An object containing the fetched data, loading status, error, and methods to control the request.

---
title: useResizeObserver
description: Custom hook that observes the size of an element using the [`ResizeObserver API`](https://developer.
category: sensors
links:
  - label: GitHub
    icon: i-simple-icons-github
    to: https://github.com/sseuniverse/sse-hooks/blob/main/packages/hooks/src/useResizeObserver
---

## Installation

::code-group

```bash [terminal]
npx sse-tool add use-resize-observer
```

```bash [npm]
npm install sse-hooks
```

::

## Usage

::code-group

```tsx [example.ts]
import { useResizeObserver } from "./{hooks file}";

const myRef = useRef(null);
const { width = 0, height = 0 } = useResizeObserver({
  ref: myRef,
  box: "content-box",
});

<div ref={myRef}>Hello, world!</div>;
```

```tsx [package.ts]
import { useResizeObserver } from "sse-hooks";

const myRef = useRef(null);
const { width = 0, height = 0 } = useResizeObserver({
  ref: myRef,
  box: "content-box",
});

<div ref={myRef}>Hello, world!</div>;
```

::

## API

### Parameters

| Name    | Type                       | Description                         |
| :------ | :------------------------- | :---------------------------------- |
| options | `UseResizeObserverOptions` | The options for the ResizeObserver. |

### Return Value

Returns `Size`.

- The size of the observed element.

## Changelog

:hooks-changelog

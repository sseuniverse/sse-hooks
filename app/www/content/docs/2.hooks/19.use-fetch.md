---
title: useFetch
description: "Custom hook that provides a wrapper around the native `fetch API` to handle HTTP requests with state management, abort capability, and TypeScript support."
category: network
links:
  - label: GitHub
    icon: i-simple-icons-github
    to: https://github.com/sseuniverse/sse-hooks/blob/main/packages/hooks/src/useFetch
  - label: MDN Docs
    icon: i-simple-icons-mdnwebdocs
    to: https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API
---

## Installation

::code-group{sync="pm"}

```bash [npm]
npx sse-hooks add use-fetch
```

```bash [yarn]
yarn dlx sse-hooks add use-fetch
```

```bash [pnpm]
pnpm dlx sse-hooks add use-fetch
```

```bash [deno]
deno run -A npm:sse-hooks add use-fetch
```

```bash [bun]
bunx sse-hooks add use-fetch
```

::

## Usage

```tsx [example.ts]
import { useFetch } from "./{hooks file}";

interface User {
  id: number;
  name: string;
}

const { data, loading, error, execute } = useFetch<User>(
  "https://api.example.com/user/1",
  {
    immediate: true,
    onSuccess: (data) => console.log("User loaded:", data),
  },
);
```

## API

### Parameters

:component-props

### Returns

:component-props{type="returns"}

### Types Aliases

:component-types

## Changelog

:hooks-changelog

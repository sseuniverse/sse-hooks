---
title: useMediaSession
description: "Custom hook that interacts with the Media Session API.
  It allows you to customize media notifications and handle media control events (like play, pause, next track) from the system's notification area or lock screen."
category: sensors
links:
  - label: GitHub
    icon: i-simple-icons-github
    to: https://github.com/sseuniverse/sse-hooks/blob/main/packages/hooks/src/useMediaSession
  - label: MDN Docs
    icon: i-simple-icons-mdnwebdocs
    to: https://developer.mozilla.org/en-US/docs/Web/API/Media_Session_API
---

## Installation

::code-group{sync="pm"}

```bash [npm]
npx sse-hooks add use-media-session
```

```bash [yarn]
yarn dlx sse-hooks add use-media-session
```

```bash [pnpm]
pnpm dlx sse-hooks add use-media-session
```

```bash [deno]
deno run -A npm:sse-hooks add use-media-session
```

```bash [bun]
bunx sse-hooks add use-media-session
```

::

## Usage

```tsx [example.ts]
import { useMediaSession } from "./{hooks file}";

const MyPlayer = () => {
  const { setMetadata, setPlaybackState } = useMediaSession({
    playbackState: "playing",
    metadata: {
      title: "Awesome Song",
      artist: "Cool Artist",
      album: "Best Hits",
      artwork: [
        { src: "/album-art.jpg", sizes: "512x512", type: "image/jpeg" },
      ],
    },
    actionHandlers: {
      play: () => console.log("Play clicked"),
      pause: () => console.log("Pause clicked"),
      nexttrack: () => console.log("Next track clicked"),
    },
  });

  return <div>Now Playing...</div>;
};
```

## API

### Parameters

:component-props

### Returns

:component-props{type="returns"}

### Types Aliases

:component-types

## Changelog

:hooks-changelog

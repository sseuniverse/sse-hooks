---
title: useIsMounted
description: "Custom hook that determines if the component is currently mounted."
category: lifecycle
links:
  - label: GitHub
    icon: i-simple-icons-github
    to: https://github.com/sseuniverse/sse-hooks/blob/main/packages/hooks/src/useIsMounted
---

## Installation

::code-group{sync="pm"}

```bash [npm]
npx sse-hooks add use-is-mounted
```

```bash [yarn]
yarn dlx sse-hooks add use-is-mounted
```

```bash [pnpm]
pnpm dlx sse-hooks add use-is-mounted
```

```bash [deno]
deno run -A npm:sse-hooks add use-is-mounted
```

```bash [bun]
bunx sse-hooks add use-is-mounted
```

::

## Usage

```tsx [example.ts]
import { useIsMounted } from "./{hooks file}";

const isComponentMounted = useIsMounted();
// Use isComponentMounted() to check if the component is currently mounted before performing certain actions.
```

## API

### Parameters

:component-props

### Returns

:component-props{type="returns"}

### Types Aliases

:component-types

## Changelog

:hooks-changelog

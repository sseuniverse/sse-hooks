---
title: useWindowSize
description: "Custom hook that tracks the size of the window."
category: sensors
links:
  - label: GitHub
    icon: i-simple-icons-github
    to: https://github.com/sseuniverse/sse-hooks/blob/main/packages/hooks/src/useWindowSize
---

## Installation

::code-group{sync="pm"}

```bash [npm]
npx sse-hooks add use-window-size
```

```bash [yarn]
yarn dlx sse-hooks add use-window-size
```

```bash [pnpm]
pnpm dlx sse-hooks add use-window-size
```

```bash [deno]
deno run -A npm:sse-hooks add use-window-size
```

```bash [bun]
bunx sse-hooks add use-window-size
```

::

## Usage

```tsx [example.ts]
import { useWindowSize } from "./{hooks file}";

const { width = 0, height = 0 } = useWindowSize();
console.log(`Window size: ${width} x ${height}`);
```

## API

### Parameters

:component-props

### Returns

:component-props{type="returns"}

### Types Aliases

:component-types

## Changelog

:hooks-changelog

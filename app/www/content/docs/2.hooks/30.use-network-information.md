---
title: useNetworkInformation
description: Custom hook that tracks the device's network connection status and details (speed, type) using the Network Information API.
navigation.badge: NEW
category: network
links:
  - label: GitHub
    icon: i-simple-icons-github
    to: https://github.com/sseuniverse/sse-hooks/blob/main/packages/hooks/src/useNetworkInformation
---

## Installation

::code-group
```bash [terminal]
npx sse-tool add use-network-information
```
```bash [npm]
npm install sse-hooks
```
::

## Usage


::code-group
```tsx [example.ts]
import { useNetworkInformation } from './{hooks file}'

const { isOnline, networkInfo } = useNetworkInformation();
* if (!isOnline) return <div>You are offline</div>;
* return (
<div>
Connection: {networkInfo?.effectiveType} ({networkInfo?.downlink} Mbps)
</div>
);
```
```tsx [package.ts]
import { useNetworkInformation } from 'sse-hooks'

const { isOnline, networkInfo } = useNetworkInformation();
* if (!isOnline) return <div>You are offline</div>;
* return (
<div>
Connection: {networkInfo?.effectiveType} ({networkInfo?.downlink} Mbps)
</div>
);
```
::

## API


### Return Value

Returns `UseNetworkInformationReturn`.

The network state, connection details, and support status.

---
title: useClickAnyWhere
description: Custom hook that handles click events anywhere on the document.
category: dom
links:
  - label: GitHub
    icon: i-simple-icons-github
    to: https://github.com/sseuniverse/sse-hooks/blob/main/packages/hooks/src/useClickAnyWhere
---

## Installation

::code-group
```bash [terminal]
npx sse-tool add use-click-any-where
```
```bash [npm]
npm install sse-hooks
```
::

## Usage


::code-group
```tsx [example.ts]
import { useClickAnyWhere } from './{hooks file}'

const handleClick = (event) => {
  console.log('Document clicked!', event);
};

// Attach click event handler to document
useClickAnywhere(handleClick);
```
```tsx [package.ts]
import { useClickAnyWhere } from 'sse-hooks'

const handleClick = (event) => {
  console.log('Document clicked!', event);
};

// Attach click event handler to document
useClickAnywhere(handleClick);
```
::

## API

### Parameters

| Name | Type | Description |
| :--- | :--- | :--- |
| handler | `Function` | The function to be called when a click event is detected anywhere on the document. |


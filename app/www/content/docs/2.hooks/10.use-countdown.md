---
title: useCountdown
description: Custom hook that manages countdown.
category: effect
links:
  - label: GitHub
    icon: i-simple-icons-github
    to: https://github.com/sseuniverse/sse-hooks/blob/main/packages/hooks/src/useCountdown
---

## Installation

::code-group

```bash [terminal]
npx sse-tool add use-countdown
```

```bash [npm]
npm install sse-hooks
```

::

## Usage

::code-group

```tsx [example.ts]
import { useCountdown } from "./{hooks file}";

const [counter, { start, stop, reset }] = useCountdown({
  countStart: 10,
  intervalMs: 1000,
  isIncrement: false,
});
```

```tsx [package.ts]
import { useCountdown } from "sse-hooks";

const [counter, { start, stop, reset }] = useCountdown({
  countStart: 10,
  intervalMs: 1000,
  isIncrement: false,
});
```

::

## API

### Parameters

| Name             | Type               | Description              |
| :--------------- | :----------------- | :----------------------- |
| countdownOptions | `CountdownOptions` | The countdown's options. |

### Return Value

Returns `any`.

An array containing the countdown's count and its controllers.

## Changelog

:hooks-changelog

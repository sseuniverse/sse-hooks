---
title: useUnmount
description: "Custom hook that runs a cleanup function when the component is unmounted."
category: lifecycle
links:
  - label: GitHub
    icon: i-simple-icons-github
    to: https://github.com/sseuniverse/sse-hooks/blob/main/packages/hooks/src/useUnmount
---

## Installation

::code-group{sync="pm"}

```bash [npm]
npx sse-hooks add use-unmount
```

```bash [yarn]
yarn dlx sse-hooks add use-unmount
```

```bash [pnpm]
pnpm dlx sse-hooks add use-unmount
```

```bash [deno]
deno run -A npm:sse-hooks add use-unmount
```

```bash [bun]
bunx sse-hooks add use-unmount
```

::

## Usage

```tsx [example.ts]
import { useUnmount } from "./{hooks file}";

useUnmount(() => {
  // Cleanup logic here
});
```

## API

### Parameters

:component-props

### Returns

:component-props{type="returns"}

### Types Aliases

:component-types

## Changelog

:hooks-changelog

---
title: useIsClient
description: "Custom hook that determines if the code is running on the client side (in the browser)."
category: lifecycle
links:
  - label: GitHub
    icon: i-simple-icons-github
    to: https://github.com/sseuniverse/sse-hooks/blob/main/packages/hooks/src/useIsClient
---

## Installation

::code-group{sync="pm"}

```bash [npm]
npx sse-hooks add use-is-client
```

```bash [yarn]
yarn dlx sse-hooks add use-is-client
```

```bash [pnpm]
pnpm dlx sse-hooks add use-is-client
```

```bash [deno]
deno run -A npm:sse-hooks add use-is-client
```

```bash [bun]
bunx sse-hooks add use-is-client
```

::

## Usage

```tsx [example.ts]
import { useIsClient } from "./{hooks file}";

const isClient = useIsClient();
// Use isClient to conditionally render or execute code specific to the client side.
```

## API

### Parameters

:component-props

### Returns

:component-props{type="returns"}

### Types Aliases

:component-types

## Changelog

:hooks-changelog

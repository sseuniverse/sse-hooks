---
title: useCounter
description: "Custom hook that manages a counter with increment, decrement, reset, and setCount functionalities."
category: state
links:
  - label: GitHub
    icon: i-simple-icons-github
    to: https://github.com/sseuniverse/sse-hooks/blob/main/packages/hooks/src/useCounter
---

## Installation

::code-group{sync="pm"}

```bash [npm]
npx sse-hooks add use-counter
```

```bash [yarn]
yarn dlx sse-hooks add use-counter
```

```bash [pnpm]
pnpm dlx sse-hooks add use-counter
```

```bash [deno]
deno run -A npm:sse-hooks add use-counter
```

```bash [bun]
bunx sse-hooks add use-counter
```

::

## Usage

```tsx [example.ts]
import { useCounter } from "./{hooks file}";

const { count, increment, decrement, reset, setCount } = useCounter(5);
```

## API

### Parameters

:component-props

### Returns

:component-props{type="returns"}

### Types Aliases

:component-types

## Changelog

:hooks-changelog

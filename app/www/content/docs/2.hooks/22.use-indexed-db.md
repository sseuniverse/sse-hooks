---
title: useIndexedDB
description: Custom hook that provides an interface to the [`IndexedDB API`](https://developer.
category: storage
links:
  - label: GitHub
    icon: i-simple-icons-github
    to: https://github.com/sseuniverse/sse-hooks/blob/main/packages/hooks/src/useIndexedDB
---

## Installation

::code-group

```bash [npm]
npx sse-hooks add use-indexed-db
```

```bash [yarn]
yarn dlx sse-hooks add use-indexed-db
```

```bash [pnpm]
pnpm dlx sse-hooks add use-indexed-db
```

```bash [deno]
deno run -A npm:sse-hooks add use-indexed-db
```

```bash [bun]
bunx sse-hooks add use-indexed-db
```

::

## Usage

```tsx [example.ts]
import { useIndexedDB } from "./{hooks file}";

interface UserProfile {
  name: string;
  age: number;
}

const { setItem, getItem, data } = useIndexedDB<UserProfile>(
  "myAppDB",
  "profiles",
);

const saveProfile = async () => {
  await setItem("user_1", { name: "Alice", age: 30 });
};
```

## API

### Parameters

| Name         | Type                  | Description                                                   |
| :----------- | :-------------------- | :------------------------------------------------------------ |
| databaseName | `string`              | The name of the IndexedDB database.                           |
| storeName    | `string`              | The name of the object store within the database.             |
| options      | `UseIndexedDBOptions` | Configuration options for the database connection (optional). |

### Return Value

Returns `UseIndexedDBReturn`.

An object containing the current data state, error state, loading state, and methods to interact with the database.

## Changelog

:hooks-changelog

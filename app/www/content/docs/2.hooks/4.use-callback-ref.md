---
title: useCallbackRef
description: A custom hook that converts a callback to a ref to avoid triggering re-renders when passed as a prop or avoid re-executing effects when passed as a dependency.
navigation.badge: NEW
category: utilities
links:
  - label: GitHub
    icon: i-simple-icons-github
    to: https://github.com/sseuniverse/sse-hooks/blob/main/packages/hooks/src/useCallbackRef
---

## Installation

::code-group

```bash [terminal]
npx sse-tool add use-callback-ref
```

```bash [npm]
npm install sse-hooks
```

::

## Usage

::code-group

```tsx [example.ts]
import { useCallbackRef } from "./{hooks file}";

const handleClick = useCallbackRef((event: MouseEvent) => {
  console.log("Clicked!", event);
});

useEffect(() => {
  window.addEventListener("click", handleClick);
  return () => window.removeEventListener("click", handleClick);
}, []);
```

```tsx [package.ts]
import { useCallbackRef } from "sse-hooks";

const handleClick = useCallbackRef((event: MouseEvent) => {
  console.log("Clicked!", event);
});

useEffect(() => {
  window.addEventListener("click", handleClick);
  return () => window.removeEventListener("click", handleClick);
}, []);
```

::

## API

### Parameters

| Name     | Type | Description |
| :------- | :--- | :---------- | ------------------------------------------------------------- |
| callback | `T   | undefined`  | The callback function to store in a ref. It can be undefined. |

### Return Value

Returns `T`.

T A stable function reference that always calls the latest callback.

## Changelog

:hooks-changelog
